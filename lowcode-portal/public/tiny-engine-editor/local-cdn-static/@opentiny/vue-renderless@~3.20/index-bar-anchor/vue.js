import "../chunk-G2ADBYYC.js";
const api = ["state"];
const renderless = (props, { computed, reactive, onMounted, onUpdated, watch }, { emit, parent, refs }) => {
  const state = reactive({});
  const api2 = {
    state
  };
  Object.assign(api2, {
    state
  });
  return api2;
};
export {
  api,
  renderless
};
